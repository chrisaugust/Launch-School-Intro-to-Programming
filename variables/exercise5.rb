# The first program prints x to the screen with the value of 3.
# The second program throws an error because of scope issues with x (x was defined within the block
# so the scope is limited to the block).
